#!/usr/bin/env python3
"""run_acceptance.py — ejecuta los checks declarados en SDD/acceptance.yaml.

Stub. Se completa en sprints posteriores con un runner real:
- Carga acceptance.yaml de la feature indicada.
- Ejecuta cada check (command, files_exist, pattern_present, pattern_absent).
- Devuelve exit 0 si todos pasan, ≠0 si alguno falla.
"""
from __future__ import annotations

import sys


def main() -> int:
    print("run_acceptance.py — placeholder. Pendiente Sprint C/E.")
    return 0


if __name__ == "__main__":
    sys.exit(main())
